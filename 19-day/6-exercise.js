// Write a JavaScript program to compute the sum of three elements
// of a given array of integers of length 3.

function arraySum(array) {
  return array[0] + array[1] + array[2];
}

console.log(arraySum([12, 34, 45]));
